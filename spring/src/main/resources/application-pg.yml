spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rankedrace
    username: dev
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

